credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "slack-token5"
              secret: '${SLACK_TOKEN}'
              description: Slack token5 # Name
          - string:
              scope: GLOBAL
              id: "gitlab-token5"
              secret: ${GITLAB_TOKEN}
              description: "GitLab personal access token5" # Name
unclassified:
  slackNotifier:
    teamDomain: test # i.e. your-company (just the workspace name not the full url)
    tokenCredentialId: "slack-token5"
    room: "dev-xxx" # channel name without "#"
