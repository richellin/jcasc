credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: slack-token2
              secret: '${SLACK_TOKEN}'
              description: Slack token # Name
          - string:
              scope: GLOBAL
              id: "gitlab-token2"
              secret: ${GITLAB_TOKEN}
              description: "GitLab personal access token" # Name
          # - string:
          #     scope: GLOBAL
          #     id: slack-token3
          #     secret: '${SLACK_TOKEN}'
          #     description: Slack token3 # Name
          # - string:
          #     scope: GLOBAL
          #     id: "gitlab-token3"
          #     secret: ${GITLAB_TOKEN}
          #     description: "GitLab personal access token3" # Name
unclassified:
  slackNotifier:
    teamDomain: test # i.e. your-company (just the workspace name not the full url)
    tokenCredentialId: slack-token2
